\documentclass[aspectratio=169]{beamer}
\usetheme{blob}
\usepackage[normalem]{ulem}

\title{Hopping on the Margins}
\subtitle{with Yifan Wang, and Quinten Weller}
\author{Panos Oikonomou}
\institute{New York University}
\date{\today}

\begin{document}

% ════════════════════════════════════════════════════════════════
% Set up the title slide with a custom background
{
  \setbeamertemplate{background}{\input{media/background}} 
  \begin{frame}[plain]
    \titlepage
  \end{frame}
}
% ════════════════════════════════════════════════════════════════


% ════════════════════════════════════════════════════════════════
% Where are Conformal defects popping up?
\section{Motivation}
\begin{frame}
  \frametitle{Defects in the wild}
  \framesubtitle{Using spin chains to pull out conformal defects}
  \begin{figure}
    \centering
    \includegraphics[width=0.8\linewidth]{media/lattice.png}
  \end{figure}
  \begin{equation*}
      H = -J\sum_{n} x_nx_{n+1} - h\sum_{n} z_n
  \end{equation*}
\end{frame}

\begin{frame}
  \frametitle{Defects in the wild}
  \framesubtitle{Using spin chains to pull out conformal defects}
  \begin{figure}
    \centering
    \includegraphics[width=0.8\linewidth]{media/lattice-defect.png}
  \end{figure}
  \begin{equation*}
      H = -J\sum_{n} x_nx_{n+1} - h\sum_{n}z_n + \delta H_{01}
  \end{equation*}
\end{frame}

\begin{frame}
  \frametitle{Defects in the wild}
  \framesubtitle{Using spin chains to pull out conformal defects}
  \begin{figure}
    \centering
    \includegraphics[width=0.8\linewidth]{media/lattice-defect-full.png}
  \end{figure}
  \begin{equation*}
      H = -J\sum_{n} x_nx_{n+1} - h\sum_{n}z_n + \delta H_{01}
  \end{equation*}
\end{frame}

\begin{frame}
  \frametitle{Defects in the wild}
  \framesubtitle{Using spin chains to pull out conformal defects}
  \begin{figure}
    \centering
    \includegraphics[width=0.8\linewidth]{media/lattice-defect-zoom-out.png}
  \end{figure}
  \begin{equation*}
      H = -J\sum_{n} x_nx_{n+1} - h\sum_{n}z_n + \delta H_{01}
  \end{equation*}
\end{frame}
% ════════════════════════════════════════════════════════════════


% ════════════════════════════════════════════════════════════════
% RG of tricritical Ising with all the variations
\begin{frame}
  \frametitle{Flowing with these defects}
  \framesubtitle{Chasing the next simplest case}
  \begin{columns}
    \begin{column}{0.1\paperwidth}
    \end{column}
    \begin{column}{0.35\paperwidth}
      \begin{minipage}[t][0.34\paperwidth][t]{0.35\paperwidth}
        \begin{figure}
          \centering
          \only<1>{\includegraphics[width=\linewidth]{media/tricritical-flows-1}}%
          \only<2>{\includegraphics[width=\linewidth]{media/tricritical-flows-2}}%
          \only<3>{\includegraphics[width=\linewidth]{media/tricritical-flows-3}}%
          \only<4>{\includegraphics[width=\linewidth]{media/tricritical-flows-4}}%
          \only<5>{\includegraphics[width=\linewidth]{media/tricritical-flows-5}}%
        \end{figure}
      \end{minipage}
    \end{column}
    \hspace{0.02\paperwidth}%
    \begin{column}{0.45\paperwidth}%
      \begin{minipage}[t][0.34\paperwidth][t]{\linewidth}
        \raggedright
        Consider $\mathcal{T} \coloneqq M(4,5)$\par 
        \textbf{\textit{Tricritical Ising Model}}%
        \vspace{0pt plus 1fill}%
        \begin{equation*}%
          \only<1>{S = S_D}%
          \only<2>{S = S_D + \lambda_{\phi}\int_{D} \phi(z) dz}%
          \only<3>{S = S_D + \lambda_{\bar \phi}\int_{D} \bar \phi(z) dz}%
          \only<4>{S = S_D + \lambda\int_{D} \phi(z) + \bar \phi(z) dz}%
          \only<5>{S = S_D + \lambda_{\phi}\int_{D} \phi(z) dz + \lambda_{\bar \phi}\int_{D} \bar \phi(z) dz}%
        \end{equation*}
        % \vspace{0pt plus 2fill}%
        \vfill
        \raggedleft
        {\tiny Results from Watts, Runkel, and co.}
      \end{minipage}
    \end{column}
    \hspace{0.07\paperwidth}
  \end{columns}
\end{frame}
% ════════════════════════════════════════════════════════════════

% ════════════════════════════════════════════════════════════════
% Titlecard yeyyy
\begin{frame}
  \frametitle{Outline}
  \framesubtitle{The story so far}
  \begin{columns}[t]
    \begin{column}{0.22\textwidth}
      \centering
      \includegraphics[width=\linewidth]{media/widget-introduction}\\[1ex]
      \tiny Motivation
    \end{column}
    \pause
    \begin{column}{0.22\textwidth}
      \centering
      \includegraphics[width=\linewidth]{media/widget-background}\\[1ex]
      \tiny Review how to describe conformal defects
    \end{column}
    \pause
    \begin{column}{0.22\textwidth}
      \centering
      \includegraphics[width=\linewidth]{media/widget-strategy}\\[1ex]
      \tiny Approach to finding fixed points
    \end{column}
    \pause
    \begin{column}{0.22\textwidth}
      \centering
      \includegraphics[width=\linewidth]{media/widget-results}\\[1ex]
      \tiny Progress and future
    \end{column}
  \end{columns}
\end{frame}
% ════════════════════════════════════════════════════════════════

% ════════════════════════════════════════════════════════════════
% Background
\begin{accentframe}{So Where are the endpoints?}{Studying the endpoints of flows by hopping and hoping.}
\end{accentframe}

\section{Finding Fixed Points}
\begin{frame}
  \frametitle{Chiral Algebras}
  \framesubtitle{The building blocks of a Conformal Field Theory}
  \centering
  \only<1-2>{\includegraphics[width=0.6\linewidth]{media/cft-vir-lego.png}}%
  \only<3>{\includegraphics[width=0.6\linewidth]{media/cft-w-lego.png}}%
  \\[2ex]%
  \visible<2->{%
    \only<1-2>{\includegraphics[width=0.7\linewidth]{media/vir-primary.pdf}}%
    \only<3>{\includegraphics[width=0.7\linewidth]{media/w-primary.pdf}}%
  }
\end{frame}


\setbeamercovered{transparent}
\begin{frame}
  \frametitle{Defects are unfolded boundaries}
  \framesubtitle{Reframing the problem in terms of Boundary CFT}
  In a theory $\mathcal{T}$ with conformal defect $\alpha$, 
  \begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{media/folding-defects.pdf}
  \end{figure}
  \pause
  Similar to how a linear map
  \begin{equation}
    \alpha : \mathbb{H}\to \mathbb{H} \longleftrightarrow \ket{\alpha} : \mathbb{C}\to \mathbb{H} \otimes \mathbb{H}^\ast.
  \end{equation}
\end{frame}

\begin{frame}
  \frametitle{Characterizing Conformal Defects}
  \framesubtitle{Useful tools}
  Invariance under translation along the defect implies
  \begin{equation}
    T_{\rm{above}}(z) - \bar T_{\rm{above}}(\bar z) = 
    T_{\rm{below}}(z) - \bar T_{\rm{below}}(\bar z)
  \end{equation}
  \pause
  For a boundary state $\alpha$ this translates to 
  \begin{equation}
    (L_n - \bar L_{-n})\ket{\alpha} = 0.
  \end{equation}
\end{frame}

\begin{frame}
  \frametitle{Characterizing Conformal Defects}
  \framesubtitle{Useful tools}
  The condition
  \begin{equation}
    (L_n - \bar L_{-n})\ket{\alpha} = 0,
  \end{equation}
  implies that...

  \begin{minipage}[t][0.5\textheight][t]{\linewidth}
    \only<1>{
      \begin{block}{Fact I}
        Boundary states are built with spinless primaries since for $n=0$
        \begin{equation}
          (L_0 - \bar L_0) \ket{\alpha} = 0 
        \end{equation}
      \end{block}
    }
    \only<2>{
      \begin{block}{Fact II}
        A basis is given by \textbf{Ishibashi states} $|i\rangle\!\rangle$ defined for a spinless primary $i$ by
        \begin{equation}
          |i\rangle\!\rangle = \sum_{n} |i,n\rangle \otimes U |i,n\rangle,
        \end{equation}with $|i,n\rangle$ an orthonormal basis of the $i$ irrep, and $U$ fixed antiunitary map.
      \end{block}
    }
    \only<3>{
      \begin{block}{Fact III}
        Each boundary state is given by 
        \begin{equation}
          \ket{\alpha} = \sum_{h_i = \bar h_i}\alpha_i|i\rangle\!\rangle,
        \end{equation}
        for some $\alpha_i \in \mathbb{C}$.
      \end{block}
    }
  \end{minipage}
\end{frame}
% ════════════════════════════════════════════════════════════════

% ════════════════════════════════════════════════════════════════
% Transition to how to solve this
\begin{frame}
  \frametitle{Outline}
  \framesubtitle{The story so far}
  \begin{columns}[t]
    \pgfsetfillopacity{0.1}
    \begin{column}{0.22\textwidth}
      \centering
      \includegraphics[width=\linewidth]{media/widget-introduction}\\[1ex]
      \tiny Motivation
    \end{column}
    \begin{column}{0.22\textwidth}
      \centering
      \includegraphics[width=\linewidth]{media/widget-background}\\[1ex]
      \tiny Review how to describe conformal defects
    \end{column}
    \pgfsetfillopacity{1}
    \begin{column}{0.22\textwidth}
      \centering
      \includegraphics[width=\linewidth]{media/widget-strategy}\\[1ex]
      \tiny Approach to finding fixed points
    \end{column}
    \pgfsetfillopacity{0.1}
    \begin{column}{0.22\textwidth}
      \centering
      \includegraphics[width=\linewidth]{media/widget-results}\\[1ex]
      \tiny Progress and future
    \end{column}
    \pgfsetfillopacity{1}
  \end{columns}
\end{frame}

\begin{frame}
  \frametitle{Strategy}
  \framesubtitle{How can we then find fixed points?}
  \begin{columns}[t]
    \begin{column}{0.35\textwidth}
      \centering
      \includegraphics[width=0.2\paperwidth, trim=0 0 0 2pt, clip]{media/widget-calculate.png}\\[1ex]
      \footnotesize Find a way to \textbf{generate} conformal defects.
    \end{column}
    \pause
    \begin{column}{0.35\textwidth}
      \centering
      \only<1>{\transparent{0.3}\includegraphics[width=0.2\paperwidth, trim=0 0 0 2pt, clip]{media/widget-constrain.png}\\[1ex]}%
      \only<2>{\includegraphics[width=0.2\paperwidth, trim=0 0 0 2pt, clip]{media/widget-constrain.png}\\[1ex]}%
      \footnotesize Use \textbf{constraints} to check which ones could be the fixed point.
    \end{column}
  \end{columns}
\end{frame}
% ════════════════════════════════════════════════════════════════

% ════════════════════════════════════════════════════════════════
% Boundary Building
\begin{accentframe}{Finding Defect Builders}{A story of cheating using rational theories and gauging.}
\end{accentframe}

\section{Generate Defects}
\begin{frame}
  \frametitle{Cardy Trick}
  \framesubtitle{A cheeky way to generate boundaries}
  \centering
  \includegraphics[width=0.8\linewidth]{media/open-closed-string-duality.pdf}
  \pause
  \begin{block}{Cardy's Condition}
    Equating the channels we have
    \begin{equation}
      \langle \alpha, e^{-\pi L H_{\tau}} \beta\rangle = Z(L) = \mathrm{Tr\,}_{\mathbb{H}_{\alpha \beta}}e^{- 2\pi H_\sigma}
    \end{equation}
  \end{block}
\end{frame}

\begin{frame}
  \frametitle{Cardy Trick}
  \framesubtitle{A cheeky way to generate boundaries}
  \begin{columns}
    \begin{column}{0.6\textwidth}
      \raggedright
      We can project into an annulus with Hamiltonian
      \begin{equation}
        H_\tau = L_0 + \bar L_0 - \frac{c}{12},
      \end{equation}
      and partition function
      \begin{equation}
        Z(L) = \langle \alpha, e^{-\pi L \left(L_0 + \bar L_0 - \frac{c}{12} \right)} \beta \rangle.
      \end{equation}
    \end{column}
    \begin{column}{0.4\textwidth}
    \end{column}
    \begin{textblock*}{0.36\textwidth}(0.6\textwidth+0.15\paperwidth, 0.00\paperheight)
      \includegraphics[width=\linewidth]{media/annulus.pdf}
    \end{textblock*}
  \end{columns}
\end{frame}

\begin{frame}
  \frametitle{Cardy Trick}
  \framesubtitle{A cheeky way to generate boundaries}
  \begin{columns}
    \begin{column}{0.6\textwidth}
      \raggedright
      But also onto an upper half plane with Hamiltonian
      \begin{equation}
        H_\sigma = -\frac{1}{L} \left(L_0  - \frac{c}{24}\right),
      \end{equation}
      and partition function
      \begin{equation}
        Z(L) = \mathrm{Tr}_{\mathbb{H}_{\alpha \beta}} e^{\frac{2\pi}{L} \left( L_0 - \frac{c}{12} \right)}.
      \end{equation}
    \end{column}
    \begin{column}{0.4\textwidth}
    \end{column}
    \begin{textblock*}{0.39\textwidth}(0.6\textwidth+0.15\paperwidth, 0.00\paperheight)
      \includegraphics[width=\linewidth]{media/strip.pdf}
    \end{textblock*}
  \end{columns}
\end{frame}

\begin{frame}
  \frametitle{Cardy Trick}
  \framesubtitle{A cheeky way to generate boundaries}
  \begin{block}{Cardy's Condition}
    Matching the two conditions and expressing into characters we find that
    \begin{equation}
      S^{ji} \alpha_j^\ast \beta_j = n_{\alpha \beta}^{i} \in \mathbb{Z}_{\geq 0}
    \end{equation}
  \end{block}
  \pause
  Which has solutions for a \textit{rational diagonal CFT} labelled by the primaries $\alpha$
  \begin{equation}
    \alpha_j = \frac{S_{\alpha j}}{\sqrt{S_{\alpha 1}}}.
  \end{equation}
  These are also a basis of \textbf{simple physical boundaries}.
\end{frame}

\begin{frame}
  \frametitle{Generating Defects}
  \framesubtitle{The recipe}
  \vfill
  \begin{columns}[c]
    \begin{column}{0.22\textwidth}
      \begin{block}{Step I}
        \footnotesize
        Fold a \textbf{CFT}
        \[
        \mathcal{T} \otimes \overline{\mathcal{T}}
        \]
      \end{block}
    \end{column}
    \pause
    \begin{column}{0.22\textwidth}
      \begin{block}{Step II}
        \footnotesize
        Make it \textbf{rational}
        \[
        \mathrm{Vir} \subset \mathcal{W}
        \]
      \end{block}
    \end{column}
    \pause
    \begin{column}{0.22\textwidth}
      \begin{block}{Step III}
        \footnotesize
        \textbf{Cardy States}
        \[
        \alpha_j = \dfrac{S_{\alpha j}}{\sqrt{S_{\alpha 0}}}
        \]
      \end{block}
    \end{column}
    \pause
    \only<1-4>{%
    \begin{column}{0.22\textwidth}
      \begin{block}{Step IV}
        \footnotesize
        \textbf{Reflect}\par 
        Are we done?
      \end{block}
    \end{column}
    }%
    \only<5>{%
    \begin{column}{0.22\textwidth}
      \begin{alertblock}{Step IV}
        \footnotesize
        \textbf{Reflect}\par 
        Are we done? \textbf{NO!}
      \end{alertblock}
    \end{column}
    }%
  \end{columns}
  \vfill
\end{frame}

\begin{frame}
  \frametitle{Fusion}
  \framesubtitle{Even more boundaries!}
  Diagonal Cardy boundaries preserve too many symmetries. But conformal boundaries don't have to.\\[1ex]
  \pause
  \begin{block}{A Fix}
  Fuse with topological defects $\mathcal{L}$ that break the big symmetry and get more boundaries
    \begin{equation}
      \mathcal{L}\ket{\alpha} = n_{\mathcal{L}\alpha}^{\beta} \ket{\beta},
    \end{equation}
  for $\beta$ simple physical boundary, $n_{\mathcal{L} \alpha}^\beta \in \mathbb{Z}_{\geq 0}$.
  \end{block}
  To do this, we use modular bootstrap to come up with topological lines.
\end{frame}

\begin{frame}
  \frametitle{Gauging}
  \framesubtitle{Even even more boundaries!}
  More boundaries? No problem!
  \begin{block}{Cool fact I}
    After gauging a discrete symmetry $G$ in theory $\mathcal{T}$ results in identifying a dual symmetry $\hat G$ in $\mathcal{T}{/}G$ such that
    \[
    (\mathcal{T}{/}G){/}\hat G = \mathcal{T}.
    \]
  \end{block}
\end{frame}
% ════════════════════════════════════════════════════════════════

% ════════════════════════════════════════════════════════════════
% Constraint Building
\begin{frame}
  \frametitle{Gauging}
  \framesubtitle{Even even more boundaries!}
  \vspace{2ex}
  \begin{block}{New Approach!}
    Find boundaries after gauging and pull them back via the operations in $\hat G$. For $\hat G$ a \textit{group} 
    \[
    \ket{\alpha}_{\mathcal{T}} = \sum_{g \in \hat G} \frac{g}{|\hat G|}\ket{\alpha}_{\mathcal{T}{/}G}
    \]
  \end{block}
  \vspace{5ex}
  {\tiny \textbf{Footnote}: One could also pull back \textit{twisted} boundary conditions.}
\end{frame}

\begin{accentframe}{Generating Constraints}{Why should this be an endpoint?}
\end{accentframe}

\section{Checking Defects}
\begin{frame}
  \frametitle{g-function}
  \framesubtitle{Easy to compute \& a-theorem esque}
  \begin{figure}
    \centering
    \includegraphics[width=0.5\linewidth]{media/g-function.pdf}
  \end{figure}
  is monotonically decreasing under RG. Universal as a boundary addition to the entropy. At high temperatures
  \begin{equation}
    S(\beta) = \frac{c \pi L}{6\beta} + 2\log g_\alpha + \cdots.
  \end{equation}
\end{frame}

\begin{frame}
  \frametitle{Preserved Symmetries}
  \framesubtitle{Along a flow generated by a primary}
  \begin{block}{Cool fact II {\footnotesize \textmd{AKA Anomaly Matching}}}
    If a symmetry operator $\mathcal{L}$ commutes with a defect perturbation $\varphi$, then the symmetry is preserved along flows.
    \[
    [\mathcal{L}, \phi] = 0 \implies [\mathcal{L}, e^{\lambda_\phi \int_D \phi}] = 0.
    \]
  \end{block}
\end{frame}

\begin{frame}
  \frametitle{Preserved Symmetries}
  \framesubtitle{In the case of minimal models}
  \begin{columns}
    \begin{column}{0.55\textwidth}
      \begin{itemize}
        \item We consider perturbations of $D_{1,2}$ under $\phi_{(1,3),0}$ and $\phi_{0,(1,3)}$.
        \item Preserve defects of the form $L_{(r,1)}$ which in Tricritical Ising give the $\mathrm{Ising}$ fusion category.
      \end{itemize}
    \end{column}
    \begin{column}{0.45\textwidth}
    \end{column}
    \begin{textblock*}{0.40\textwidth}(0.57\textwidth+0.14\paperwidth, 0.25\paperheight)
      \includegraphics[width=\linewidth]{media/tricritical-flows.pdf}
    \end{textblock*}
  \end{columns}
\end{frame}
% ════════════════════════════════════════════════════════════════

% ════════════════════════════════════════════════════════════════
% What we did so far
\begin{frame}
  \frametitle{Outline}
  \framesubtitle{The story so far}
  \begin{columns}[t]
    \pgfsetfillopacity{0.1}
    \begin{column}{0.22\textwidth}
      \centering
      \includegraphics[width=\linewidth]{media/widget-introduction}\\[1ex]
      \tiny Motivation
    \end{column}
    \begin{column}{0.22\textwidth}
      \centering
      \includegraphics[width=\linewidth]{media/widget-background}\\[1ex]
      \tiny Review how to describe conformal defects
    \end{column}
    \begin{column}{0.22\textwidth}
      \centering
      \includegraphics[width=\linewidth]{media/widget-strategy}\\[1ex]
      \tiny Approach to finding fixed points
    \end{column}
    \pgfsetfillopacity{1}
    \begin{column}{0.22\textwidth}
      \centering
      \includegraphics[width=\linewidth]{media/widget-results}\\[1ex]
      \tiny Progress and future
    \end{column}
  \end{columns}
\end{frame}

\section{Status}
\subsection{Strategy}
\begin{frame}
  \frametitle{Strategy}
  \framesubtitle{Guesses for Gaugings}
  \begin{block}{Lukewarm Fact}
      Folded Tricritical Ising ($\mathrm{TIM}^2$) has $c = \frac{7}{5}$ and is rational under $\mathrm{Vir}^2$.
  \end{block}
  \pause
  Known rational CFTs with $c = \frac{7}{5}$ are the chiral algebras of:
  \begin{align}
    \mathcal{A} \coloneqq \frac{\mathrm{SU}(2)_8}{U(1)} && \mathcal{B} \coloneqq \frac{\mathrm{SU}(2)_2 \times \mathrm{SU}(2)_8}{\mathrm{SU}(2)_{10}} && \mathcal{C} \coloneqq \frac{\mathrm{Sp}(6)_1\times \mathrm{Sp}(6)_1}{\mathrm{Sp}(6)_2}.
  \end{align}
\end{frame}

\begin{frame}
  \frametitle{Gruntwork}
  \framesubtitle{Guesses for Gaugings}
  Look at all these gaugings!
  \begin{align*}
        &\mathrm{TIM}^2  \xrightarrow[]{\mathrm{id} \oplus \eta_1 \eta_2} \mathcal{B}_{DE}\,,\  
\mathrm{TIM}^2  \xrightarrow[]{\mathrm{id} \oplus S} \mathcal{A}_{D}\,,\  \mathrm{TIM}^2  \xrightarrow[]{\mathrm{id} \oplus S \oplus \eta_1\eta_2\oplus S \eta_1\eta_2} \mathcal{A}_{A}
\\
&\mathcal{B}_{AA} \xrightarrow[]{\mathrm{id}   \oplus \mathcal{L}_{(0,0,3)}} \mathcal{B}_{AE}\,,\  
\mathrm{TIM}^2  \xrightarrow[]{(\mathrm{id} \oplus S \oplus \eta_1\eta_2\oplus S \eta_1\eta_2)_\omega} \mathcal{B}_{AE}\,,\  
\mathcal{B}_{AE} \xrightarrow[]{\mathrm{id} \oplus \mathcal{L}_{(0,4,0)}}\mathcal{B}_{DE} \\ 
&\mathcal{B}_{AA} \xrightarrow[]{\mathrm{id} \oplus \mathcal{L}_{(0,4,0)}}\mathcal{B}_{DA} \,, \  \mathcal{C} \xrightarrow[]{\mathrm{id} \oplus \mathcal{L}_{2}\,,\ \mathrm{id} \oplus \mathcal{L}_{3}} \mathcal{C}' \,, \  \mathcal{C} \xrightarrow[]{\mathrm{id} \oplus \mathcal{L}_{8}} \mathcal{B}_{DE}\,, \  \mathcal{A}_{A} \xrightarrow[]{\mathbb{Z}_2}\mathcal{A}_{D}
  \end{align*}%
  \begin{itemize}%
    \pause%
    \item We verified them numerically (and in some cases analytically).
    \pause
    \item Found Cardy states for the diagonal ones.
    \pause
    \item Calculated Pullbacks of characters. 
  \end{itemize}
\end{frame}

\begin{frame}
  \frametitle{Bootstrap}
  \framesubtitle{Can't have a 2D CFT project without bootstrap}  
  We developed a solver for the modular bootstrap that uses $S,T,Z$ data to solve for the \textbf{all TDLs} in constrained rational CFTs.
  
  \only<1>{<insert mathematica photo>}
  \only<2>{
  We used this for
  \begin{enumerate}
      \item Calculating TDLs for nondiagonal rational chiral algebras in the previous models
      \item Obtaining over 400 Conformal defects in these theories (some of which we pulled back)
      \item Calculated candidate defects that match numerical predictions for $\mathrm{C}$
  \end{enumerate}
  }
\end{frame}
% ════════════════════════════════════════════════════════════════

\end{document}
